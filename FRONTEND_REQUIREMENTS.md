# RecoverHand 上位机界面与交互需求

> 文件名保留为 `FRONTEND_REQUIREMENTS.md`，以兼容既有链接。本文件描述产品界面、运行时接口和交接验收条件，不描述人员分工，也不限定具体执行器机械方案。

## 1. 产品目标与边界

RecoverHand 用于医护或研究人员监督下的手功能康复研究，主流程为：

```text
EEG 运动意图 → 意图冻结 → 安全检查 → 辅助手套执行
                                      ↓
                    sEMG 响应观察 ← 动作保持与释放
```

闭环分为两层：

- 试次内闭环：静息基线、动作提示、EEG 意图窗口、意图冻结、安全检查、辅助运动、保持、释放恢复。
- 试次间闭环：结合 EEG 意图、sEMG 响应、手套执行结果和异常记录，为下一试次提供参数调整依据；调整结果经过人工确认后生效。

界面用于设备连接、流程操作、状态呈现、异常处理和会话复核。算法推导、具体手套执行器结构和设备维护工具不属于界面主流程。当前系统仍是研究原型，算法输出不能直接解释为诊断、疗效或患者主动参与结论。

## 2. 当前实现基线

| 模块 | 当前状态 | 交接说明 |
|---|---|---|
| 原生桌面界面 | 已实现 | Python 3.11、PySide6 Qt Widgets/QSS、PyQtGraph、SQLite |
| 页面与会话导航 | 已实现 | 首页、患者与方案、设备检查、个体标定、训练、报告、设置；状态机限制跳步 |
| 设备连接档案 | 已实现 | EEG、sEMG、辅助手套可选择模拟或实验性真实驱动 |
| 会话运行时 | 已实现骨架 | 统一时钟、事件总线、试次状态机、安全监督器、记录与回放 |
| 训练页数据接入 | 已接入 | 通过运行时事件显示 EEG、sEMG、意图、相位与虚拟手状态 |
| EEG 个体标定 | 尚未接入真实流程 | 标定页仍为模拟；真实基线锁定与质量门控尚未形成完整交互 |
| sEMG 响应分析 | 已有基线实现 | `EmgResponseAnalyzer` 已进入试次闭环，当前试次结果只记录响应布尔值 |
| 辅助动作策略 | 已有基线实现 | `AssistStrategy` 将冻结意图映射为设备动作；具体设备方案不是产品界面依赖 |
| 运行时暂停/继续 | 尚未完成 | 当前暂停按钮只改变会话级界面状态，没有暂停采集循环和试次计时 |
| 报告与回放 | 有页面骨架和记录读取能力 | 尚未形成完整的试次列表、指标解释、异常复核和回放交互 |
| 三维虚拟手 | 已接入 | 用于反馈呈现；不代表患者真实关节角度、握力或康复效果 |

设备型号专用的参数写入、点动和维护控制不构成主线页面需求。实验性维护入口可以保留，但应与患者训练流程分离，并根据驱动能力动态隐藏。

## 3. 页面与交互需求

### 3.1 首页

- 呈现系统当前状态、最近会话和主流程入口。
- 明确区分“模拟联调”“真实设备实验”“可开始训练”三种状态。
- 未完成设备检查和标定时，不提供直接进入训练的捷径。

### 3.2 患者与训练方案

- 支持匿名研究编号、患侧、训练协议、目标重复次数和备注。
- 会话开始时冻结方案、设备档案、算法版本、阈值和相位参数。
- 训练中的参数变更形成事件记录，不覆盖原始会话配置。

### 3.3 设备检查

- 分别呈现 EEG、sEMG 和辅助手套的连接、数据流、时间同步、信号质量、控制就绪及故障原因。
- “已连接”“有数据”“信号可用”“允许控制”采用不同状态，不合并成单一绿色指示。
- 设备刚连接后的短暂初始化显示为过渡状态，避免直接判为故障。
- 型号专用调试控制与患者训练入口相互隔离；主流程只保留连接、检查和安全状态确认。

### 3.4 个体标定

- 包含静息基线、动作提示、意图窗口和结果复核。
- 标定结果至少包括通道质量、有效数据比例、基线是否锁定、特征可分性、算法版本和失败原因。
- 标定失败时允许重新贴电极、重新采集或退出会话，不自动降低阈值继续训练。
- 当前模拟步骤由真实 EEG 数据和意图提取结果替换后，才可标记为真实标定。

### 3.5 训练

- 当前试次相位是页面的主要视觉状态，依次显示静息、提示、意图窗口、意图冻结、安全检查、辅助、保持、释放恢复和完成/中止。
- EEG 意图在辅助动作前冻结；运动中及运动后的信号不改变当前试次意图。
- EEG、sEMG 和手套数据显示单位、时间窗、信号质量和数据来源，模拟数据带有持续可见的标识。
- 虚拟手优先显示手套实际状态；缺少实际状态时，目标值与估计值使用不同视觉样式。
- 停止训练后保留当前试次、停止原因和设备释放确认状态。
- 暂停/继续在运行时支持之前保持不可用或明确标为“仅暂停界面”，避免造成采集已暂停的误解。

### 3.6 报告与人工复核

- 按试次展示相位完整性、冻结意图、置信度、sEMG 响应、手套执行结果和异常。
- 区分原始观察、算法输出、人工判断和临床结论。
- 支持从 `metadata.json`、`events.jsonl` 和 `finalized.json` 恢复会话，并按记录顺序回放。
- 未完成或异常终止的会话保留记录，同时显示“未正常结束”。
- 会话级汇总至少包括有效试次数、无效/中止原因分布、信号质量概览和需要复核的事件。

### 3.7 系统设置

- 管理设备连接档案、默认相位时长、数据目录和显示选项。
- 连接档案只描述连接方式，不代表设备已通过本次会话检查。
- 训练期间锁定影响采集、算法和动作安全的配置。

## 4. 运行时数据接口

Qt 主线程通过 `DeviceWorker.runtime_event` 接收 `RuntimeEvent` 字典。运行时本身还提供 `SessionRuntime.subscribe()` 和 `SessionRuntime.snapshot()`，分别用于事件订阅与当前状态快照。

统一事件信封：

```python
{
    "name": str,
    "ts_ns": int,
    "source": str,
    "data": dict,
}
```

当前稳定事件：

| 事件 | 主要数据 | 界面用途 |
|---|---|---|
| `session.started` / `session.stopped` | 会话开始；停止原因 | 全局会话状态 |
| `trial.started` | `repetition_index` | 新试次初始化 |
| `trial.phase_changed` | `previous`、`current`，中止时含 `reason` | 相位导航与提示 |
| `intent.frozen` | `state`、`grip`、`confidence`、算法信息 | 冻结意图与虚拟手目标 |
| `safety.command_authorized` | `command_id`、`lease_id` | 动作授权状态 |
| `command.ack` | `ok`、`fault_code`、回显状态 | 执行结果与异常提示 |
| `safety.release` | `reason`、`was_armed` | 软件安全状态；不等同于物理释放确认 |
| `data.eeg` / `data.emg` / `data.glove` | 驱动预览数据 | 波形、质量和设备状态 |
| `trial.completed` | `phase_reached`、`intent_state`、`emg_responded` | 试次摘要 |

界面状态由事件和快照派生，不通过 QWidget 定时器生成科学数据或推进硬件时序。设备操作通过 `DeviceWorker.submit(...)` 进入设备线程，界面控件不直接持有驱动对象。

## 5. 算法接口与当前限制

`recoverhand_host.algorithms` 暴露三个可替换协议：

- `IntentExtractor`：原始 EEG → 意图结果。
- `EmgResponseAnalyzer`：sEMG 预览流 → 响应判定与指标。
- `AssistStrategy`：冻结意图 → 辅助动作和释放动作序列。

`EmgResponseAnalyzer` 和 `AssistStrategy` 可注入 `SessionRuntime.run(...)`，桌面工作线程也会转发这两个对象。`IntentExtractor` 当前由 EEG 驱动构造，尚不是 `SessionRuntime.run(...)` 的直接参数。

另外两项接口缺口会影响完整报告：

- `trial.completed` 目前只保存 `emg_responded`，尚未附带 `EmgResponseAnalyzer.metrics()`。
- 真实 EEG 驱动尚未由标定流程触发 `lock_baseline()`，因此真实个体基线闭环仍未完成。

协议签名、预览数据格式和最小接入示例见 [`backend/recoverhand_host/algorithms/README.md`](backend/recoverhand_host/algorithms/README.md)。

## 6. 安全与数据呈现边界

- 真实动作仅在设备控制就绪、会话状态允许、人工确认和本地安全条件满足时开放。
- 所有训练动作经过运行时安全监督器；型号专用维护命令不进入常规训练流程。
- `safety.release` 表示软件监督状态已经释放，只有收到设备状态或命令应答后，界面才显示“物理设备已释放”。
- 软件停止不能替代机械快拆、独立断电、硬件限位、固件看门狗或现场监督。
- 原始数据和事件时间按接收内容记录；滤波、去基线和缩放只改变显示时，需要保留处理标识。
- 单通道腕部 sEMG 容易受运动和电极位移影响，“检测到响应”属于算法观察，不直接等同于主动参与。
- 真实人体训练不属于当前软件交接验收范围。

## 7. 交接优先级

### P0：状态真实、停止可信

- 模拟与真实数据来源持续可见。
- 运行时故障、命令失败和设备断线能够进入统一异常状态。
- 软件释放、命令应答和设备实际释放使用不同状态呈现。
- 型号专用维护控制从主训练流程中隐藏或分离。

### P1：形成可用训练闭环

- 真实 EEG 标定与基线锁定。
- 运行时级暂停/继续语义。
- 相位时长配置和会话配置冻结。
- 报告页读取真实记录，显示 sEMG 指标、手套结果和异常，并支持回放。

### P2：操作体验与健壮性

- 断线重连、串口占用、BLE 扫描失败和信号质量下降的分级提示。
- 对长文本、不同窗口尺寸和高 DPI 的布局适配。
- 键盘焦点、颜色对比度和危险操作确认的一致性。
- 长时运行下的波形降采样、队列上限和界面刷新性能。

## 8. 交接验收

一次界面交接至少覆盖以下场景：

1. 纯模拟档案完成一轮会话，页面状态与 `events.jsonl` 顺序一致。
2. 任一设备连接失败时，故障原因可见且不能误进入“可训练”。
3. 训练中停止后形成结束记录，界面不把软件释放事件当作设备物理释放证明。
4. 异常终止会话可以在报告页识别并复核。
5. 界面线程无设备阻塞；连续运行时波形、三维手和事件列表保持可操作。
6. 自动化测试、静态检查和编译检查全部通过。

```powershell
.\.venv\Scripts\python.exe -m pytest -q
.\.venv\Scripts\python.exe -m ruff check backend tests
.\.venv\Scripts\python.exe -m compileall -q backend tests
python debug_algorithms.py all
```
